Requirement 1: Added the screen for the user to type a message with a button labele "Send Message."


Requirement 2: In this commit, I added EchoActivity, a second screen that displays the user’s input using a TextView. I also added a centered “I got the message” button that calls finish() to return to InputActivity while preserving the typed text through the Android activity lifecycle and configuration changes such as screen rotation.
